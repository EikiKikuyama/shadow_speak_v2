plugins {
    id "com.android.application"
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    signingConfigs {
        release {
            storeFile file("/Users/kikuyama/my-release-key.jks")
            storePassword "And1new0831"
            keyAlias "my-key-alias"
            keyPassword "And1new0831"
        }
    }

    buildTypes {
        release {
            signingConfig signingConfigs.release
        }
    }

    namespace = "com.shadow.speak"
    compileSdk = flutter.compileSdkVersion
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = "1.8"
    }

    defaultConfig {
    applicationId = "com.shadow.speak"
    minSdkVersion 24
    targetSdkVersion flutter.targetSdkVersion

    versionCode 2        // 👈 明示的に「2」に変更！
    versionName "1.0.1"  // 👈 わかりやすく名前も変えておくとベター
}

}

flutter {
    source = "../.."
}

import 'package:flutter/material.dart';

class RecordingOnlyMode extends StatelessWidget {
  const RecordingOnlyMode({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Recording Only Mode"),
    );
  }
}

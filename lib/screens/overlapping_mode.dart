import 'package:flutter/material.dart';

class OverlappingMode extends StatelessWidget {
  const OverlappingMode({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Overlapping Mode"),
    );
  }
}

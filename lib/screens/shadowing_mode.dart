import 'package:flutter/material.dart';

class ShadowingMode extends StatelessWidget {
  const ShadowingMode({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Shadowing Mode"),
    );
  }
}

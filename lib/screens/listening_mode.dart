import 'package:flutter/material.dart';

class ListeningMode extends StatelessWidget {
  const ListeningMode({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Listening Mode"),
    );
  }
}
